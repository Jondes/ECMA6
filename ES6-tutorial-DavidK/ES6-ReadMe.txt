A transpiler reads code written in one language and 
produces the equivalent code in another.

1.Webpack bundles modules into one .js file
2.Comes with a dev-server

Polyfill
is a piece of code(or plugin) that provides the technology 
that you expect to provide natively.
__________________________________________________________________________
Section1: Essentials in Javascript
Block scoping binds variable declarations to blocks of code, { .. }.

The let keyword replaces var in es6.

The const keyword declares a variable that cannot be re-assigned.

Template literals in es6 are strings with embedded variables or code inside. 
They have surrounding backticks `` with interpolated ${} symbols for variables

